// ltl-minesweeper/main.ltl
//
// Terminal minesweeper with solver and 3BV scoring.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-minesweeper v0.1.0")
    io::println("Terminal minesweeper with solver and 3BV scoring.")
    0
}
